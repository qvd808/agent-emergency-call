[package]
name = "agent"
version = "0.1.0"
edition.workspace = true

[dependencies]
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "net", "io-util"] }
turn = { path = "../turn" }
