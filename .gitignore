# Secrets (CONTEXT.md: never commit secrets)
.env

# Local-only
CONTEXT.md

# Per-machine Claude Code overrides
.claude/settings.local.json

# Subagent git worktrees
.claude/worktrees/

# Rust build output
/target/

# Call logs, one JSON file per call; the directory itself is kept
/calls/*
!/calls/.gitkeep
