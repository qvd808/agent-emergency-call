; RTP: the ports Asterisk takes audio streams from (issue #14).

[general]
; Must match the range docker-compose.yml publishes. The built-in default, 5000 to 31000
; (rtp.conf.sample), is far wider than anything worth publishing through Docker Desktop.
rtpstart=10000
rtpend=10099
