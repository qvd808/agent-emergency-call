; Logging (issue #14). Asterisk runs in the foreground (`asterisk -f`), so the console is the
; container's output, which `make asterisk-logs` follows. Nothing is written to a log file.

[logfiles]
console => notice,warning,error,verbose
